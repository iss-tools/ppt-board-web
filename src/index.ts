export { default as Main } from './components/Main.vue';

// Re-export everything from core so plugins only need to depend on this package
export * from '@iss-ai/vue-canvas-core';
export * from '@iss-ai/ppt-board';
